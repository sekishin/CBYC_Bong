package code;

import java.awt.Color;


public class Player extends Character {
    // コンストラクタ
    public Player(int x, int y, int w, int h, int m, Color c, String p) {
        super(x, y, w, h, m, c, p);
    }
}
